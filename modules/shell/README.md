# `/modules/shell/`

## What is a Shell?
A Shell is an ephemeral set of tools that lives in a given environment.

## What are the usecases for a Shell?
Specific dev environment, the use of tools that you would only rarely use.
